# Plan — S.7 S11 on the engine (walking skeleton)

**Spec:** `docs/superpowers/specs/2026-07-06-s7-engine-s11-design.md`

1. `yee_engine::sparams::reflection_db(dut_p1, ref_p1, dt_s, freqs)` — panics on length
   mismatch (caller error, same jobs → same lengths); unit test with a synthetic 0.25
   reflection (−12.04 dB).
2. Extend `tests/sparams_stub_notch.rs`: add a P1 probe (under the trace, between drive
   and stub) to both runs; compute |S11|(f); assert |S11|(f_notch) ≥ −4 dB and
   |S11|² + |S21|² at the notch ∈ [0.5, 1.3]; print the measured values.
3. Verify: fmt, clippy `-D warnings`, fast tests, release gate re-run (~2 × 80 s),
   record measured numbers.
4. Ship: ADR-0184, roadmap S.7 row + footer, SUMMARY.md, commit + push.
