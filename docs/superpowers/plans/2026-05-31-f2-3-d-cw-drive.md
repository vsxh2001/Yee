# Filter Phase F2.3-d — CW per-frequency drive — Plan

**Spec:** `2026-05-31-f2-3-d-cw-drive-design.md` · **ADR:** ADR-0128

## Lane
`crates/yee-voxel/**` ONLY (`src/lumped_sim.rs` + module doc). Do NOT edit
yee-fdtd/yee-filter. Out of lane → finding. (Cargo.lock/ci.yml from the main-merge
expected.)

## Base / worktree
Existing worktree `worktrees/lumped-fdtd`, branch `feature/filter-f2-3-lumped-fdtd`
(tip `ab0f5a6` — has the aperture-port wiring + the air-gap fix). **FIRST: merge
current `main`** (it has the cap diagnostic + the 6.x work). Cargo.lock `--theirs`
+ `cargo check` in container; ci.yml keep ALL gate jobs. Commit the merge.

## Pattern files (READ FIRST)
- `crates/yee-voxel/src/lumped_sim.rs` — the CURRENT driver: `run_board_solve`
  (the modulated-Gaussian + broadband DFT you replace with CW), the aperture-port
  placement (KEEP it). The DUT/thru normalization (KEEP — it divides out the line).
- `crates/yee-fdtd/tests/cap_cw_001.rs` (READ-ONLY) — how to drive a CW sinusoid
  with a Hann ramp + measure a steady-state amplitude over a settled window. Mirror
  its CW drive + steady-state measurement.
- `docs/src/decisions/0128-...md` (the CW method + the bounded frequency set),
  ADR-0127 (the cap/port is correct under CW — the tank resonates).

## Steps
1. Merge `main` into the F2.3 branch (Cargo.lock --theirs, keep all ci.yml jobs);
   `cargo check -p yee-voxel` green in container.
2. Add a CW per-frequency drive to `run_board_solve` (or a CW variant): for each
   `f`, CW sinusoid (Hann-ramped first ~M cycles) for `n_settle` cycles (Q≈10 ⇒
   ~10–30 + line transit), measure the steady-state load-voltage amplitude over the
   last few cycles. DUT + thru per `f`; `S21(f)=|V_dut,ss|/|V_thru,ss|`.
3. Frequency set = the gate points (2.0 GHz, 2.4 GHz) + a handful for shape (NOT a
   fine sweep — bound the runtime).
4. Re-run `fdtd_lumped_001` in the container; capture the CW |S21| at the gate
   frequencies. Update the module doc.

## Verify (bounded container — heavy FDTD, multiple CW solves)
- `YEE_BOX_DIR=/home/hadassi/Code/Yee/worktrees/lumped-fdtd ... scripts/yee-box.sh
  bash -c 'cargo fmt --check -p yee-voxel && cargo clippy -p yee-voxel --all-targets
  -- -D warnings'` → exit 0.
- `... scripts/yee-box.sh cargo test -p yee-voxel --release --test fdtd_lumped_001
  -- --ignored --nocapture` → REPORT the CW |S21| + GREEN-or-how-close. (Generous
  timeout; if N CW solves are too slow, trim the frequency set to the gate points +
  2–3 and say so.)
- (cargo direct or `bash -c`, NEVER `bash -lc`.)

## Escape hatch
Do NOT weaken `fdtd_lumped_001` to force GREEN. If, on a CW steady-state drive, the
band-pass still doesn't meet the loose tol, record the achieved CW |S21| precisely
(passband IL + stopband rejection in dB, does a peak/notch form now?) — a precise
"how close under CW" finding is the honest outcome (and would point to a residual
port-accuracy or geometry issue, not the measurement). Blocked > 60 min on the
merge/build/runtime → surface. Do NOT touch yee-fdtd/yee-filter.

## Done when
fmt/clippy clean; `fdtd_lumped_001` re-run with the CW drive + the |S21| reported.
Either GREEN (→ branch ready for review + the F2.3 merge, EM-sim ships 5/6) OR a
precise "how close under CW" finding. diff = `crates/yee-voxel/**` (+ merge
artifacts).
