# FS.4.0 — multilayer stackup walking skeleton (plan)

**Spec:** `docs/superpowers/specs/2026-07-12-fs4-stackup-design.md`

1. `yee-layout`: `Stackup` + `StackupLayer` (serde like `Substrate`),
   `Stackup::symmetric_stripline(eps_r, b_m)` convenience; unit tests.
2. `yee-voxel`: `voxelize_stackup(layout, &stackup, trace_layer, opts)`;
   gate `voxel-stackup-001` (single-layer ≡ microstrip bit-identical;
   3-layer band/lid/no-gap checks) in `tests/voxel_stackup_001.rs`.
3. `yee-engine/tests/stripline_eeff.rs`: gate `engine-stripline-eeff-001`
   (release, ignored; picked up by the blanket engine CI step — name has
   no skipped prefix); measure once, pin.
4. ADR-0215 + SUMMARY line; roadmap FS.4 row.
5. Verify: fast tests + clippy floor + fmt; the release gate boxed run.
