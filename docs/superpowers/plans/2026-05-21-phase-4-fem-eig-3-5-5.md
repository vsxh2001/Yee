# Phase 4.fem.eig.3.5.5 — implementation plan

**Spec:** `docs/superpowers/specs/2026-05-21-phase-4-fem-eig-3-5-5-design.md`
**Base SHA:** `d6611d2` (post Phase 4.fem.eig.3.5.4 merge)
**Lane:** `crates/yee-validation/src/lib.rs`,
`crates/yee-validation/tests/fem_eig_006_high_aspect_pml.rs`,
`docs/src/tutorials/07-fem-open-cavity.md`, `ROADMAP.md`,
`docs/src/decisions/0049-*.md` (if a retire/escape-hatch ADR is
warranted). Out of lane: `crates/yee-fem/**` (the multi-mode API is
frozen from v3.5.4 — if a defect surfaces, surface as a finding,
do NOT fix here).

## Step ladder

### N1 — Frequency retune + measure

1. `crates/yee-validation/src/lib.rs`: `FEM_EIG_006_F_HZ` `30.0e9`
   → `40.0e9`. Update the doc-comment with the §2.2 cutoff table
   (TE_{10} prop / TE_{20} prop / TE_{01} evanescent at 40 GHz).
2. No closure change expected (the `fem_eig_006_beta_te20`
   `arg > 0` branch fires automatically at 40 GHz). Confirm by
   reading the closure.

**Verification:**
```
cargo test --release -p yee-validation --test fem_eig_006_high_aspect_pml \
  -- --include-ignored --nocapture 2>&1 | tail -20
```
Capture `|S_{11}|(40 GHz)`.

### N2 — Gate disposition

**If `|S_{11}| < 0.1`:** remove `#[ignore]`, rewrite docstring as
"Phase 4.fem.eig.3.5.5 retire" with the 40 GHz measurement +
matched-modal-termination rationale. Update the gate-(A) panic
message.

**Else (escape-hatch):** keep `#[ignore]`, log the 40 GHz
measurement in the docstring, queue v3.5.6. If `|S_{11}|` is
plausibly discretisation-limited (risk §4 a), run one refinement
probe (`NY`/`NZ` bump) before deciding — measure, don't
speculate.

**Verification:**
```
cargo test --release -p yee-validation --test fem_eig_006_high_aspect_pml 2>&1 | tail -8
```
Default path green (gate retired-and-passing OR ignored).

### N3 — Docs + ROADMAP

1. `docs/src/tutorials/07-fem-open-cavity.md`: add "Phase
   4.fem.eig.3.5.5" subsection under the v3.5.4 subsection. Add a
   fourth row to the measurement table for 40 GHz. State retire or
   escape-hatch.
2. `ROADMAP.md`: new v3.5.5 line; if retired, mark fem-eig-006
   **closed**.
3. ADR-0049 only if escape-hatch (record the v3.5.6 decision);
   skip if the gate retires cleanly (the retire is self-documenting
   via the test docstring + ROADMAP).

**Verification:** `mdbook build docs/` exit 0;
`grep -n "Phase 4.fem.eig.3.5.5" ROADMAP.md docs/src/tutorials/07-fem-open-cavity.md`
≥ 2 matches.

## Lint floor (every commit)

```bash
cargo fmt --check --all
cargo clippy --workspace --all-targets -- -D warnings
```

## Escape-hatch

Blocked > 20 min on a single step → surface and stop. If the gate
does not retire at 40 GHz even after one mesh-refinement probe,
do NOT chase it further in v3.5.5 — commit the measurement, keep
`#[ignore]`, and write ADR-0049 queuing v3.5.6 (absorbing-mode
wave-port, ADR-0048 Option (b)).

## Out-of-scope (surface as findings, do not fix)

* `crates/yee-fem/**` multi-mode API internals.
* yee-py / Python frequency or multi-mode kwarg.
* fem-eig-003 driver (its v3.5.2 retire stands).
